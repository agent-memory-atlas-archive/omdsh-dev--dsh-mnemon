# Workspace Strategy

A complete optional Strategy for composing runtime context, project notes, journals, tasks, playbooks and collaboration Sources. It selects public operations, allocates one shared budget and accepts contributions through its own extension SDK. Pure composition never writes storage or dispatches work.

The default three-tier Strategy remains available; selecting this Strategy is explicit. Run `pnpm verify` for standalone checks.
